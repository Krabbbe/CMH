import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Line 1")
            Text("Line 2")
        }
    }
}

struct SettingsView_Previews: PreviewProvider {
    static var previews: some View {
        SettingsView()
    }
}
